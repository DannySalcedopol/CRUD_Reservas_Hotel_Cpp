MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
    , ui(new Ui::MainWindow)
{
    ui->setupUi(this);

    ui->tablaReservas->horizontalHeader()
        ->setSectionResizeMode(QHeaderView::Stretch);
}
